---
description: 'A storefront app.'
labels: ['app', 'react']
---

A React application for storefront. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use storefront 
```

To run the app locally, run the following:

```
bit run storefront 
```
